# Firebase
FIREBASE_PROJECT_ID=gerador-curriculos
# For local development you may set FIREBASE_SERVICE_ACCOUNT to a path, but avoid committing service account files.
# FIREBASE_SERVICE_ACCOUNT=../gerador-curriculos-firebase-adminsdk-fbsvc-03cfb95235.json
# For production/CI use the full JSON contents in the env var `FIREBASE_SERVICE_ACCOUNT_JSON` or configure Application Default Credentials.

# AbacatePay
ABACATE_API_KEY=sk_test_xxx
ABACATE_WEBHOOK_SECRET=whsec_xxx

# Admin
ADMIN_KEY=some-strong-secret

# Storage / PDF worker
PDF_BUCKET=orders
PDF_EXPIRES_SECONDS=3600

PORT=8080
